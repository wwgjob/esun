package com.ntust.application.worker;

public class SingletonTest {
    private final Singleton singleton;


    public SingletonTest(Singleton singleton) {
        this.singleton = singleton;
    }
}

